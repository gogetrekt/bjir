{
  "$schema": "https://json.schemastore.org/package.json",
  "name": "@bjir/router",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "BJIR Gateway — local multi-provider AI gateway (OpenAI-compatible, streaming proxy)",
  "license": "MIT",
  "scripts": {
    "typecheck": "tsgo --noEmit",
    "dev": "bun run src/index.ts",
    "start": "bun run src/index.ts"
  },
  "exports": {
    ".": "./src/index.ts"
  },
  "dependencies": {
    "hono": "catalog:"
  },
  "devDependencies": {
    "@tsconfig/node22": "catalog:",
    "@types/bun": "catalog:",
    "@types/node": "catalog:",
    "typescript": "catalog:",
    "@typescript/native-preview": "catalog:"
  }
}
